# Daily Working Notes - 2026-05-30

## Production Stuck On Older Build

Alex reported the new version still looked stuck.

Checks:

- Local branch: `live-results-polish`
- Latest local/public commit: `798e453 Simplify search flow and update milestones`
- `public/main` was already at `798e453`.
- `private/main` was behind at `415ebcf Add remaining tasks review document`.
- Production homepage initially still showed old-build indicators:
  - `Updates` nav link
  - `Example recommendation` card

Action:

- Verified git author was the GitHub owner identity:
  - `Octapolis <181661051+Octapolis@users.noreply.github.com>`
- Ran `npm run typecheck`.
- Ran `npm run lint`.
- Pushed current `HEAD` to `private/main`.

Result:

- Production homepage updated and no longer showed the old `Updates` nav or `Example recommendation` card.
- `npm run smoke:deploy` passed for `https://avalon-reach.vercel.app`.

Likely cause:

- Vercel was watching or otherwise depending on the private GitHub repository path, while only `public/main` had the newest commit.

Follow-up:

- Keep pushing deployment-ready commits to both `public/main` and `private/main` unless Vercel's connected repository is confirmed and the unused remote is retired.
