# 2026-05-25 Working Notes

Week: 4  
Theme: live testing, documentation system, and recommendation cleanup

## Live Site Smoke Test

Context:

- Week 4 began after the live Vercel launch.
- The immediate question was whether the deployed site actually worked for a real search.
- A public address was used instead of a private personal address.

Test target:

- Site: https://avalon-reach.vercel.app/
- Test address: `1400 John F Kennedy Blvd, Philadelphia, PA 19107`

Actions tried:

- Checked homepage response.
- Loaded results page with address and priority query string.
- Called `/api/search` with the test address.
- Checked provider handoff route `/go/verizon`.
- Checked `/privacy` and `/terms`.

Result:

- Homepage returned HTTP 200.
- Results page rendered.
- `/api/search` returned HTTP 200.
- Live lookup geocoded the address to Philadelphia, PA.
- Live broadband lookup returned real provider results:
  - Verizon
  - Xfinity
  - Hotwire Communications
  - Starlink
  - AT&T
  - HughesNet
  - T-Mobile
  - MINTernet
  - Viasat
- `/go/verizon`, `/privacy`, and `/terms` returned HTTP 200.

Issue / blocker:

- Live broadband data includes speed/technology but not reliable pricing.
- Best-value mode currently shows awkward `0.0 Mbps per dollar` / score `0` when price is missing.
- Duplicate provider entries can appear for different technologies, such as Xfinity cable and Xfinity fiber.
- Lead capture storage was not tested during this pass to avoid creating unnecessary live lead records.

Decision:

- Treat the live app as working but needing Week 4 refinement.
- Prioritize missing-price handling before broader feature expansion.

Progress updates:

- `../../progress/plan-progress.md`: Week 4 live testing started.
- `../../progress/known-issues.md`: missing price, duplicate provider display, lead-capture verification, and referral-link gaps tracked.
- `../../progress/suggestions.md`: missing-price fallback copy and 5-10 address test checklist suggested.

Next:

- Update recommendation logic and UI copy:
  - If price is missing, do not calculate Mbps-per-dollar.
  - Show "price unavailable" or "pricing must be confirmed."
  - For best-value mode with missing price, fall back to speed + transport quality and explain the fallback.
- Create a 5-10 address live-test checklist.
- Decide on a controlled test email before testing lead capture in production.

## Documentation Structure

Context:

- Weekly status reports are useful for summaries, but they do not capture the detailed engineering trail.
- The final report, resume writeup, and future build process need both polished status and raw working notes.

Actions tried:

- Created `docs/project-notes/` for weekly project reporting.
- Created `docs/project-notes/working-notes/` for action-by-action project history.
- Added Week 1-4 status reports.
- Expanded Week 3 status into final-report/resume-ready language.
- Added `docs/project-notes/process-map.md`.
- Added daily working notes under `docs/project-notes/working-notes/daily/`.
- Added `docs/project-notes/progress/` for maintained progress, suggestions, and known issues.

Result:

- The project now has four documentation layers:
  - status reports for course summaries
  - daily working notes for engineering history
  - progress trackers for current project state
  - process map for future repeatability

Issue / blocker:

- Earlier weeks are reconstructed from memory and commits rather than written day-by-day at the time.

Decision:

- Use daily working notes going forward whenever a test, failed path, blocker, or decision happens.
- In the same flow, update progress files when a note creates/changes an issue, suggestion, or plan status.

Progress updates:

- `../../progress/plan-progress.md`: documentation system marked as active Week 4 project-management work.
- `../../progress/suggestions.md`: update-flow rule added.

Next:

- Keep adding entries as Week 4 fixes and tests happen.
- Use the daily working notes as raw material for weekly status reports and final report sections.

## Project Covenant / Mission Document

Context:

- The project needed a durable north-star document so the mission, ideal customer, value promise, process rules, and reset/handoff flow do not get scattered across weekly notes.
- The goal is not only to finish CMIT 450, but to create a reusable structure someone could use to continue AvalonReach or replicate the process for a new site.

Actions tried:

- Added `docs/project-notes/project-covenant.md`.
- Connected the covenant to the process map, README, and progress update rules.

Result:

- The project now has a mission/customer/promise document plus clear operating rules.
- The covenant defines how to reset or hand off the project without rebuilding the working process from memory.

Issue / blocker:

- The covenant should be revisited if the ideal customer, product scope, or operating process changes.

Decision:

- Treat `project-covenant.md` as the first document to read when project direction gets unclear.

Progress updates:

- `../../progress/suggestions.md`: add/update rule for mission or process changes.
- `../../progress/plan-progress.md`: documentation engine expanded as Week 4 project-management progress.

Next:

- Use the covenant before adding major features so scope stays aligned with the product promise.

## AI Recovery Document

Context:

- Alex wanted a recovery document for a total AI context wipe, model reset, or handoff.
- The document should let a new assistant get back into useful action as if it had never seen AvalonReach.

Actions tried:

- Added `docs/project-notes/ai-recovery.md`.
- Linked it from the README, process map, and covenant reset rules.

Result:

- AvalonReach now has a first-read recovery guide with repo paths, live URL, current phase, top issues, read order, commands, important files, and the current recommended next action.

Issue / blocker:

- The recovery guide must stay current when the top issue or phase changes.

Decision:

- Treat `ai-recovery.md` as the first document after context loss or handoff.

Progress updates:

- `../../progress/suggestions.md`: recovery guide should be maintained when phase/top issue changes.

Next:

- Update `ai-recovery.md` whenever current phase, live status, top issues, or recommended next action changes.

## Week 3 Report Intake

Context:

- Alex uploaded the formal Week 3 progress report after the project-notes system was created.
- The report is source material for the final paper, resume/project narrative, and process-map documentation.

Actions tried:

- Extracted text from the uploaded `.docx`.
- Added searchable source notes at `../../source-reports/week-03-progress-report-2.md`.
- Updated `../../week-03-status.md` with facts from the report.
- Updated `../week-03-working-notes.md` with a 2026-05-24 report-intake entry.
- Added a scope-alignment issue to `../../progress/known-issues.md`.
- Added a suggestion to align visible recommendation language with the fastest-plan MVP framing.

Result:

- Week 3 status now matches the submitted report more closely.
- The official course-report framing is preserved: public MVP focuses on fastest available internet plan, while other recommendation categories remain future expansion unless intentionally reintroduced.

Issue / blocker:

- Current app/docs still mention broader priority modes in places, which may conflict with the Week 3 report's fastest-plan-first MVP story.

Decision:

- Treat the mismatch as a Week 4 tuning/documentation issue.

Next:

- During Week 4 recommendation cleanup, decide whether to simplify visible product copy around fastest plan or clearly mark broader recommendation modes as experimental/roadmap features.

## Week 4 Testing-First Direction

Context:

- Alex clarified that because the app already has built features, Week 4 should not downgrade or remove them just to match the Week 3 report wording.
- The preferred order is testing and fixing existing features first, then moving into Week 4 upgrades and tuning.

Actions tried:

- Inspected `package.json` scripts.
- Checked for existing test/spec files; none are currently present.
- Ran local baseline checks:
  - `npm run typecheck`
  - `npm run build`
  - `npm run lint`
- Ran live smoke checks against:
  - `/`
  - `/results`
  - `/api/search`
  - `/go/verizon`
  - `/privacy`
  - `/terms`

Result:

- `npm run typecheck` passed.
- `npm run build` passed.
- `npm run lint` failed before linting code because `next lint` is not working with the installed Next version.
- Live homepage returned HTTP 200.
- Live results page returned HTTP 200 for the public Philadelphia test address.
- Live API search returned HTTP 200 for POST with the test address and priority.
- Direct GET to `/api/search` returned HTTP 405, which matches the current POST-only implementation.
- `/go/verizon`, `/privacy`, and `/terms` returned HTTP 200.
- Repaired lint tooling by adding `eslint.config.mjs` and changing the lint script to `eslint .`.
- Lint then found three internal navigation anchors in `app/layout.tsx`; replaced them with Next `Link`.
- Reran `npm run lint`, `npm run typecheck`, and `npm run build`; all passed.

Issue / blocker:

- No dedicated automated test suite exists yet.
- Existing recommendation modes need testing and copy alignment, not removal.

Decision:

- Week 4 sequence is now:
  - baseline-test existing features
  - fix broken checks/behaviors
  - then continue upgrades and tuning
- Preserve already-built priority modes unless testing proves a mode is broken or misleading.

Progress updates:

- `../../progress/plan-progress.md`: Week 4 order changed to testing-first.
- `../../progress/known-issues.md`: added and fixed lint tooling issue.
- `../../progress/suggestions.md`: added testing-first suggestion.
- `../../week-04-status.md`: added baseline check results and POST/GET API distinction.
- `../../ai-recovery.md`: updated current state and next action.

Next:

- Continue testing existing priority modes and lead capture.
- Fix missing-price display/scoring after baseline checks are stable.

## Priority Mode Matrix Baseline

Context:

- After Alex confirmed existing features should not be downgraded, the next test was to run each built priority mode against the same public baseline address.

Test target:

- Site: https://avalon-reach.vercel.app/
- API: `POST /api/search`
- Address: `1400 John F Kennedy Blvd, Philadelphia, PA 19107`

Actions tried:

- Posted the same address to `/api/search` with each priority:
  - `best-value`
  - `fastest`
  - `cheapest`
  - `upload`
  - `gaming`

Result:

- All five API calls returned provider results.
- `fastest` ranked Verizon Fiber first with 2300 Mbps download.
- `upload` ranked Verizon Fiber first with 2300 Mbps upload.
- `gaming` ranked fiber providers first by transport profile.
- `best-value` returned score `0` and copy saying `0.0 Mbps per dollar` because price data is missing.
- `cheapest` returned score `0` and budget copy even though price data is missing for the tested live results.

Issue / blocker:

- AR-I001 should cover both `best-value` and `cheapest` missing-price behavior.
- Gaming mode passes functionally, but the visible score label should eventually be clearer because score `7` represents transport rank, not latency or Mbps.

Decision:

- Treat `fastest`, `upload`, and `gaming` as baseline-pass for this address.
- Treat `best-value` and `cheapest` as needing missing-price fixes before broader Week 4 upgrades.

Progress updates:

- `../../testing/week-04-existing-feature-test-plan.md`: priority matrix updated with baseline results.
- `../../progress/known-issues.md`: AR-I001 expanded to include cheapest mode.
- `../../progress/plan-progress.md`: priority matrix marked complete for baseline address.
- `../../week-04-status.md`: baseline matrix summary added.

Next:

- Get approved lead-capture test email or temporarily defer lead capture.
- Fix missing-price behavior for `best-value` and `cheapest`.

## Missing-Price Recommendation Fix

Context:

- Alex asked to focus on the missing-price/missing-results issue first.
- The immediate problem was not missing provider results; results existed, but best-value and cheapest displayed misleading zero scores when live pricing was unavailable.

Actions tried:

- Updated `lib/recommendation.ts`.
- Updated `lib/types.ts`.
- Updated `app/results/page.tsx`.

Implementation:

- `valueScore` is now `null` when `estimatedMonthlyPrice` is unavailable.
- Added `scoreLabel` to ranked provider results.
- Results cards now show `scoreLabel` instead of `Score {number}`.
- Best-value mode uses speed plus transport quality as a fallback when pricing is unavailable.
- Cheapest mode no longer treats missing prices as zero or free; it explains that cheapest cannot be confirmed without listed monthly pricing.

Verification:

- `npm run lint` passed.
- `npm run typecheck` passed.
- `npm run build` passed.
- Started local dev server on port 3015 for verification.
- Local `POST /api/search` priority matrix returned:
  - `best-value`: `Price unavailable`, no fake value score.
  - `cheapest`: `Price unavailable`, no fake budget score.
  - `fastest`: speed score still works.
  - `upload`: upload score still works.
  - `gaming`: connection-fit label still works.
- Local results page no longer contains `0.0 Mbps per dollar` or `Score 0` for the tested address.

Result:

- AR-I001 is fixed locally.
- Public Vercel still needs deployment and live retest before the issue can be fully closed.

Progress updates:

- `../../testing/week-04-existing-feature-test-plan.md`: marked best-value and cheapest fixed locally / retest after deploy.
- `../../progress/known-issues.md`: AR-I001 updated with local fix details.
- `../../progress/plan-progress.md`: missing-price fix marked fixed locally.
- `../../week-04-status.md`: local fix noted.
- `../../ai-recovery.md`: current top issue updated.

Next:

- Decide whether to deploy this fix now.
- After deployment, rerun the live priority matrix for best-value and cheapest.

## Duplicate Provider Label Fix

Context:

- After the missing-price fix, Alex wanted to move to the next fix before upload/deployment.
- The next live-test issue was duplicate-looking provider cards, such as Xfinity appearing for both cable and fiber.

Decision:

- Do not remove provider/technology variants yet.
- Treat separate technologies as valid distinct results, but make the distinction visible in the card heading and recommendation text.

Actions tried:

- Updated `app/results/page.tsx`.
- Updated `lib/recommendation.ts`.

Implementation:

- If a ranked provider has no `planName`, the result card title now uses provider plus transport label.
- Example labels:
  - `Xfinity Cable / DOCSIS`
  - `Xfinity Fiber / FTTP`
- Recommendation explanations now include the transport label when no plan name is available.

Verification:

- `npm run lint` passed.
- `npm run typecheck` passed.
- `npm run build` passed.
- Local API result explanations include technology labels for no-plan live results.
- Local results page for the baseline address contains `Xfinity Cable / DOCSIS` and `Xfinity Fiber / FTTP`, and no bare duplicate `Xfinity</h3>` headings.

Result:

- AR-I002 is fixed locally.
- Public Vercel still needs deployment and live retest before the issue can be fully closed.

Progress updates:

- `../../testing/week-04-existing-feature-test-plan.md`: duplicate provider behavior marked fixed locally / retest after deployment.
- `../../progress/known-issues.md`: AR-I002 updated with local fix details.
- `../../progress/plan-progress.md`: duplicate provider label fix marked fixed locally.
- `../../week-04-status.md`: local fix noted.
- `../../ai-recovery.md`: current issue state updated.

Next:

- Decide whether to deploy the local fixes now or continue with one more pre-upload fix.

## Provider Handoff Clarity Fix

Context:

- Alex approved one more pre-upload fix: provider handoff clarity.
- `/go/[provider]` already returned HTTP 200, but the copy was too placeholder/developer-facing.

Decision:

- Keep the handoff route.
- Do not pretend production referral links are configured.
- Make the user-facing copy honest and useful.

Actions tried:

- Updated `app/go/[provider]/page.tsx`.
- Updated `app/styles.css` for a small button row.

Implementation:

- Handoff page now says AvalonReach does not have a live referral link for the provider yet.
- It tells users to confirm availability, monthly price, promos, fees, and installation details directly with the provider.
- It keeps two actions:
  - get update alerts
  - search another address

Verification:

- `npm run lint` passed.
- `npm run typecheck` passed.
- `npm run build` passed.
- Local `/go/verizon` returned HTTP 200.
- Local rendered page contained the new handoff copy/actions.

Result:

- AR-I003 is fixed locally for MVP honesty.
- Real provider/referral links remain a later production data task.
- Public Vercel still needs deployment and live retest before the issue can be fully closed.

Progress updates:

- `../../testing/week-04-existing-feature-test-plan.md`: provider links marked fixed locally / retest after deployment.
- `../../progress/known-issues.md`: AR-I003 updated with local fix details.
- `../../progress/plan-progress.md`: handoff clarity fix marked fixed locally.
- `../../week-04-status.md`: local fix noted.
- `../../ai-recovery.md`: current issue state updated.

Next:

- Deploy or upload the local fixes when approved.
- Retest live best-value, cheapest, duplicate provider labels, and `/go/verizon`.

## Price Clarity Fix

Context:

- Alex asked to look at price clarity next.
- Missing-price scoring was already fixed locally, but result cards did not have a dedicated monthly-price line.

Decision:

- Make price status visible on every provider card.
- Keep score labels for ranking context, but show price separately so users understand whether a real listed monthly price is available.

Actions tried:

- Updated `lib/types.ts`.
- Updated `lib/recommendation.ts`.
- Updated `app/results/page.tsx`.
- Updated `app/page.tsx`.
- Updated `app/styles.css`.

Implementation:

- Added `priceLabel` to ranked provider results.
- Cards now display `Listed price: $X/mo` when `estimatedMonthlyPrice` exists.
- Cards now display `Listed price: unavailable - confirm with provider` when live pricing is missing.
- Homepage sample cards now show the fallback sample prices.

Verification:

- `npm run lint` passed.
- `npm run typecheck` passed.
- `npm run build` passed.
- Local API for the baseline live address returned `Listed price: unavailable - confirm with provider`.
- Local results page displayed unavailable price labels for live-style results.
- Local homepage displayed listed sample prices such as `$75/mo` and `$55/mo`.

Result:

- Price clarity is fixed locally.
- Public Vercel still needs deployment and live retest before the issue can be fully closed.

Progress updates:

- `../../testing/week-04-existing-feature-test-plan.md`: price line clarity marked fixed locally / retest after deployment.
- `../../progress/known-issues.md`: AR-I008 added.
- `../../progress/plan-progress.md`: price clarity fix marked fixed locally.
- `../../week-04-status.md`: local fix noted.

Next:

- Deploy or upload the local fixes when approved.
- Retest live best-value, cheapest, price lines, duplicate provider labels, and `/go/verizon`.
